# Webrtc Blockchain Messaging

> A webrtc messinger that uses a blockchain

## Initialize And Run

Install dependencies

```bash
yarn install
cd client && yarn install
```

Run

```bash
yarn start
cd client && start
```